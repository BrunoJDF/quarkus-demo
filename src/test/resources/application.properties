# Test configuration for integration tests
# Provide the RestClient config key used by your ExchangeClient
quarkus.rest-client.ex-change-client.url=http://localhost:8080
quarkus.rest-client.ex-change-client.api-key=test-api-key

# You can add other test-specific overrides here to avoid requiring real external services
# For example:
# quarkus.http.port=8081
# quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/testdb

